---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: synologyreverseproxies.proxy.hnet.io
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
spec:
  group: proxy.hnet.io
  names:
    kind: SynologyReverseProxy
    listKind: SynologyReverseProxyList
    plural: synologyreverseproxies
    singular: synologyreverseproxy
    shortNames:
      - srp
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Source
          type: string
          jsonPath: .spec.sourceHostname
        - name: Dest
          type: string
          jsonPath: .spec.destHostname
        - name: UUID
          type: string
          jsonPath: .status.uuid
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
      schema:
        openAPIV3Schema:
          description: SynologyReverseProxy is the Schema for the synologyreverseproxies API
          type: object
          properties:
            apiVersion:
              type: string
            kind:
              type: string
            metadata:
              type: object
            spec:
              description: SynologyReverseProxySpec defines the desired state
              type: object
              required:
                - description
                - sourceHostname
                - destHostname
                - destPort
              properties:
                description:
                  description: Synology record name/description
                  type: string
                sourceHostname:
                  description: Frontend FQDN
                  type: string
                sourcePort:
                  description: Frontend port
                  type: integer
                  default: 443
                sourceProtocol:
                  description: Frontend protocol
                  type: string
                  enum: [http, https]
                  default: https
                destHostname:
                  description: Backend IP or hostname
                  type: string
                destPort:
                  description: Backend port
                  type: integer
                destProtocol:
                  description: Backend protocol
                  type: string
                  enum: [http, https]
                  default: http
                aclProfile:
                  description: Optional ACL profile name
                  type: string
                assignCertificate:
                  description: Auto-assign matching wildcard certificate
                  type: boolean
                  default: false
            status:
              description: SynologyReverseProxyStatus defines the observed state
              type: object
              properties:
                uuid:
                  description: Synology reverse proxy record UUID
                  type: string
                certId:
                  description: Certificate ID assigned to this proxy record
                  type: string
                conditions:
                  description: Conditions represent the latest available observations
                  type: array
                  items:
                    description: Condition contains details for one aspect of the current state
                    type: object
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    properties:
                      lastTransitionTime:
                        type: string
                        format: date-time
                      message:
                        type: string
                        maxLength: 32768
                      observedGeneration:
                        type: integer
                        format: int64
                        minimum: 0
                      reason:
                        type: string
                        maxLength: 1024
                        minLength: 1
                        pattern: '^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$'
                      status:
                        type: string
                        enum: ["True", "False", Unknown]
                      type:
                        type: string
                        maxLength: 316
                        pattern: '^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$'
