# Binaries
bin/
dist/
# Built binary (not the config/ directory)
bin/manager

# Go build cache
*.test
*.out
cover.out

# Vendor (if not using modules)
vendor/

# IDE
.idea/
.vscode/
*.swp
*.swo

# macOS
.DS_Store

# Kubernetes
kubeconfig
*.kubeconfig

# Secrets — never commit real credentials
*.env
.env
secrets.yaml
secret.yaml

# Helm
*.tgz

# envtest binaries downloaded by setup-envtest
bin/envtest/
